# Envelope Mastering Suite v1.0.0 ユーザーガイド

## 1. このアプリについて

Envelope Mastering Suite（EMS）は、SunoなどのAI生成音源を主対象に、音質と音圧感の両方を整える単一HTMLマスタリングアプリです。旧Music Envelope Leveler（MEL）の時間方向レベリングを中核とし、Tonal Envelope Matcher（TEM）由来のスペクトル補正、適応型コンプレッサー、3帯域リバーブを一つのオフライン処理チェーンへ統合しています。

設計上の特徴は、リアルタイム処理を前提としないことです。曲全体をメモリ上で解析できるため、その瞬間の信号だけで判断する一般的なリアルタイム・エフェクトとは異なり、曲全体の統計、長時間の音量推移、処理後LUFS、ピーク余裕を利用して補正を決めます。

本アプリが目指すものは次の2点です。

- 音質: スペクトルの偏り、サブソニック、局所的な帯域過剰、空間の不足を整える
- 音圧感: 曲中の音量ムラとピーク対ボディの関係を整え、原則として-14 LUFS付近の範囲内で密度を得る

単純にピークを潰して音量を上げるマキシマイザーではありません。最終的な音楽判断はA/B/C試聴と外部メーターを併用してください。

## 2. 動作環境、プライバシー、ファイル

### 2.1 推奨環境

- Windows 11
- Google ChromeまたはMicrosoft Edgeの現行版
- 十分な空きメモリ

`index.html`を直接開いて使用できます。静的Webサーバー上でも動作します。バッチ処理で保存先フォルダへ直接書き込む機能はFile System Access APIを使うため、Chrome / Edgeを推奨します。

### 2.2 ローカル処理

デコード、解析、DSP、グラフ、WAV生成はブラウザ内で実行します。音源やプリセットを外部サーバーへ送信しません。ユーザープリセットとユーザー・リファレンスモデルはブラウザのローカルストレージへ保存されます。

### 2.3 入力

- WAV: PCM 8 / 16 / 24 / 32-bit、IEEE Float 32-bit
- MP3: ブラウザの`decodeAudioData()`でデコード
- FLAC: ブラウザがデコードできる場合に読み込み可能
- その他の音声: ブラウザがデコードできれば読み込みを試みます。WMAなどブラウザ標準でデコードできない形式は正式対応外です

### 2.4 出力

- 32-bit Float WAV
- 32 / 24 / 16 / 8-bit PCM WAV
- 「入力と同じ形式 / 標準形式」

MP3入力で「入力と同じ形式 / 標準形式」を選んだ場合は16-bit PCM WAVになります。元ファイルは変更せず、常に新しいWAVを書き出します。

16-bit PCM出力には、既定でTPDFディザーを加えます。これは16-bit量子化時の歪みをランダム化し、フェードアウトや静かな残響尾部のざらつきを抑えるためのものです。24 / 32-bit PCMと32-bit float出力には作用しません。

### 2.5 メモリ

入力、中間段、最終段、試聴差分、スナップショット等をメモリへ保持します。長時間音源、高サンプルレート、多チャンネル、長いリバーブテール、比較Cの保持は必要メモリを増やします。大きなPCM音源は長尺モードとなり、内部プレビューWAVを作らず、現在位置から75秒だけを試聴します。このモードでは比較Cと処理段前後試聴を利用できません。バッチ処理は1曲ずつ順次処理し、波形描画を止めて安定性を優先します。標準フローからバッチを開始すると、標準フローの音源、B/C、試聴キャッシュを解放します。

## 3. 4段の処理順

```text
入力音源
  1. トーンEQ
  2. エンベロープ・レベラー
  3. マスタリング・コンプレッサー
  4. マルチバンド・リバーブ
  5. 最終ピーク／Integrated LUFS参考測定
  6. 必要に応じてピークまたはラウドネス正規化
  7. WAVエンコード
```

トーンEQを先に置く理由は、EQが帯域エネルギーと音量推移を変えるためです。EQ後の信号をレベラーが解析すれば、その変化を含めて時間方向の音量を整えられます。コンプレッサーはレベリング後の信号でピークと胴体を再配分し、リバーブは最後に空間成分を加えます。

各段は独立してON/OFFできます。「入力から再処理」は必ず元の入力音源から現在の設定で全段を作り直すため、前回結果への重ね掛けは起きません。

## 4. 画面構成

### 4.1 左ペイン

- 標準フロー
- トーンEQ
- レベラー
- コンプレッサー
- リバーブ
- バッチ処理
- ログ・全体設定

標準フローは通常作業を1画面で完結させる場所です。詳細タブは、処理段固有のパラメータ、グラフ、処理段前後の試聴、ユーザープリセットを扱います。

`Shift + G`は、バッチ処理タブ以外のどこにいても「補正処理を実施」または「入力から再処理」を起動するショートカットです。プリセットなどのプルダウン、スライダー、チェックボックスにフォーカスがあっても利用できます。テキスト入力欄・テキストエリアを編集中には作動しません。

### 4.2 右ペイン上部の共通波形

タブを切り替えても次の3波形を表示します。

- 補正前A
- 最終処理後B
- 差分: 最終処理後－補正前

標準フローの再生位置と連動します。詳細タブの処理段試聴は独立した試聴経路なので、この共通波形の赤い再生位置を動かしません。

### 4.3 右ペイン下部

選択タブに応じて内容が変わります。

- 標準フロー: 解析結果、レベラー概要、トーンEQ概要、コンプレッサー概要、リバーブ概要
- トーンEQ: 入力／リファレンス／各補正段、補正カーブ、メトリクス
- レベラー: 包絡、ゲイン、前後差分、レベリング後－基準カーブ
- コンプレッサー: 検出音量、閾値、LUFS一致後、ゲインリダクション、前後差分
- リバーブ: 全体およびLow / Mid / HighのWetリボン
- バッチ: 処理設定、進捗、要確認ステータス
- ログ・全体設定: 実行ログ、正規化保存ピーク、ラウドネス基準値、出力形式、16-bitディザー

## 5. 標準フロー

### 5.1 補正対象ファイル

ファイルを選択した時点では補正を開始しません。デコードと表示準備を行い、各処理段の設定を確認できる状態にします。

### 5.2 処理段とプリセット

各カード左上のチェックボックスで処理段を有効化します。プリセットを選ぶとパラメータは変更されますが、音声へはまだ適用されません。

トーンEQだけは2つのプルダウンを持ちます。

1. リファレンスモデル
2. 補正オプション

両者は別の概念です。モデルは「どんな平均スペクトルへ向かうか」、補正オプションは「どれだけ、どの制約で近づけるか」を決めます。モデルと補正オプションを一体のプリセットとして保存しません。

公開版の既定リファレンスは「なし」です。万人向けの初期状態として強いモデル寄せを避けつつ、ラウドネス効率化EQと有効にした手動Bell EQは利用できます。

### 5.3 補正処理を実施／入力から再処理

最初は「補正処理を実施」、処理済みになった後は「入力から再処理」と表示します。進捗は11段階を基本に、解析、各DSP、後処理、WAV準備を段階表示します。処理中は設定をロックし、処理途中のUI変更が結果へ混ざることを防ぎます。

処理完了後に設定を変更すると、現在の音声結果と画面設定が一致しない「未適用」状態になります。もう一度「入力から再処理」を押して反映してください。

### 5.4 解析結果

ファイル名、時間、形式、目標、各段のピーク、Integrated LUFS参考値、現在カーブ等を一覧表示します。LUFSは比較とバッチ警告のための参考実装です。

## 6. トーンEQ

### 6.1 目的

トーンEQは、広い帯域の音色傾向をリファレンスへ近づける処理と、局所的な過剰、サブソニック、ユーザー指定の問題帯域を順番に処理します。単純な数バンドEQではなく、曲全体の高解像度スペクトルを解析して制約付きカーブを作ります。

トーンEQ内部の順序は次のとおりです。

```text
入力スペクトル解析
  → Tonal Envelope補正（モデル寄せ）
  → Residual Correction EQ（局所残差）
  → ラウドネス効率化EQ（サブソニック整理）
  → 手動Bell EQ
```

### 6.2 高解像度スペクトル解析

- 対象帯域: 20 Hz～20 kHz
- 解像度: 1/24オクターブ
- FFTサイズ: 8192
- チャンネル集約: チャンネル別パワーの平均
- 測定カーブ平滑化: 1/6オクターブ
- 曲ごとの最大解析フレーム: 96
- 広帯域レベルの基準: 80～12000 Hzの中央値

固定Hz間隔ではなく対数周波数上で処理するため、低域にも十分な解析点を割り当てます。表示グラフは0 dB線を明示し、補正前、モデル、各段後、補正中心周波数を重ねます。

リファレンスモデルを使うTonal Envelope補正とResidual Correction EQは、40 kHz以上のサンプリングレートを必要とします。40 kHz未満では「リファレンスモデルなし」を選び、ラウドネス効率化EQと手動Bell EQだけで処理できます。

### 6.3 リファレンスモデル

リファレンスモデルは、正規化された目標スペクトルと曲間IQR（四分位範囲）を持ちます。絶対ラウドネスをコピーするものではなく、周波数方向の相対的な形を表します。内蔵モデルはジャンル別傾向を与えますが、音源や意図に合わない場合は「なし」またはユーザーモデルを使います。

「リファレンスモデルなし」では次を強制的に無効化します。

- Tonal Envelope補正
- Residual Correction EQ

次は引き続き利用できます。

- ラウドネス効率化EQ
- 手動Bell EQ

リファレンスがCD音源由来でも、サブソニックを保存すべきとは限りません。そのためラウドネス効率化EQはリファレンス一致とは独立した後段として動作します。

### 6.4 Tonal Envelope補正

入力とモデルの差をそのまま細かく反転するのではなく、次の制約を通して広い音色傾向だけを抽出します。

- 補正量
- 最大ブースト
- 最大カット
- 最小特徴幅
- 最大傾斜
- 平滑化

制約後の周波数応答から最小位相FIRを構成します。内部設計グリッドは32768点、適用FIRは8192タップです。最小位相化により線形位相FIRのような大きなプリリンギングと遅延を避けつつ、通常の少数バンドEQより滑らかで高解像度な補正を行います。

補正量を上げすぎると、リファレンスの個性を強くコピーして元音源の意図を失うことがあります。まず低い補正量でA/Bし、必要な場合だけ強めてください。

### 6.5 Residual Correction EQ

Tonal Envelope適用後にも残る局所的な盛り上がりを検出し、最大5本のBell EQ候補として表示します。これは広いモデル寄せで取り切れない狭帯域の過剰を扱う段です。

- 基本はカット方向
- 最小残差と補正量から自動検出
- 周波数、ゲイン、Q、ON/OFFを編集可能
- 「再検出」で現在の条件から候補を作り直す

Residual CorrectionをOFFにすると、予測グラフからもその補正を除外します。手動Bell EQのラインとラベルはResidualのON/OFFに依存せず表示されます。

### 6.6 ラウドネス効率化EQ

Suno音源で問題になりやすい、可聴上の寄与が小さいのにピーク／エネルギー余裕を消費するサブソニックを整理する段です。20～35 Hzと45～100 Hzの実音源エネルギーを比較し、必要な場合だけハイパスを自動設定します。

- 自動サブソニック整理: 既定ON
- 最大カットオフ: 既定30 Hz
- 傾斜: 12 dB/octまたは24 dB/oct
- 自動判断が不要ならOFF可能

24 dB/octは2段を直列にして実現します。単にリファレンスに低域があるかではなく、入力音源自身の低域関係から必要性を判断します。目的は低音を薄くすることではなく、聴感に寄与しにくい超低域を除き、後段のレベラー／コンプレッサーと最終ピーク余裕を有効に使うことです。

### 6.7 手動Bell EQ

自動処理後へ2本のピーキングEQを追加できます。各バンドは周波数、ゲイン、Q、ON/OFFを持ちます。

- Bell 1: 既定OFF
- Bell 2: 10000 Hz、-3 dB、Q=2、既定OFF

Bell 2の初期値は、Suno音源で現れやすい高域過剰を試すための出発点です。音源に不要なら有効化しないでください。数値編集時は重い音声再処理を行わず、予測グラフだけを軽量更新します。実音声へは「入力から再処理」で反映します。

### 6.8 補正オプション・プリセット

補正オプションはTonal Envelope、Residual、ラウドネス効率化EQ、手動Bell EQの設定を保存します。リファレンスモデル選択は保存しません。これにより、同じ補正の強さを異なるモデルへ適用できます。

ユーザー補正オプションの拡張子は`.emstoneeq`です。

### 6.9 ユーザー・リファレンスモデル作成

トーンEQタブ最下部で、複数曲から独自モデルを作れます。

1. 同じ方向性の音源を3曲以上選びます。
2. 目標カーブ平滑化を1/4、1/3、1/2オクターブから選びます。
3. モデル名と説明を入力します。
4. 「特徴マップを作成」を押します。
5. 統計中央値、曲間IQR、平滑化済み目標を確認します。
6. 現在のモデルとして使用するか、`.emsreference`へ書き出します。

異なるジャンル、年代、マスタリング傾向を無造作に混ぜると、特徴の薄い平均になります。用途の近い曲を選び、最低3曲、できればより多くの音源からモデルを作ってください。解析条件は通常操作では固定し、モデル間の互換性を保ちます。

## 7. エンベロープ・レベラー

### 7.1 役割

レベラーはEMSの中核です。EQの後で曲中の短期ピーク包絡を解析し、不自然な区間上昇・下降、局所的な飛び出し、長い区間の音圧差を、滑らかな時間可変ゲインで整えます。

通常のコンプレッサーが瞬間信号と閾値からゲインを決めるのに対し、レベラーは曲全体の包絡から「この時刻が曲全体の流れに対してどの程度高い／低いか」を判断します。小節やセクションをまたぐ音量ムラの補正に向きます。

### 7.2 解析と基準カーブ

1. 短い窓で絶対ピーク包絡を測定
2. 外れ値を抑制
3. 中央値／平滑化処理で基準となる長期推移を作成
4. 目標ピークとの差から基準ゲインを計算
5. 補正率、ブースト／カット上限、変化速度等を適用してメインゲインを作成
6. サンプル時刻へ補間して全チャンネルへ同じゲインを適用

左右へ同じゲインを適用するため、元のステレオ定位を不用意に動かしません。

### 7.3 主な設定

- 目標ピーク: 包絡を置く基準レベル
- 解析窓／ホップ: 時間解像度
- 中央値・平滑化: 基準カーブの滑らかさ
- 補正率: 基準カーブへ近づける割合
- 最大ブースト／最大カット: ゲイン量の安全上限
- 変化速度: 急なゲイン移動を制限
- 飛び出し抑制: レベリング後に残る局所ピークを追加整理

強い補正は曲の意図したダイナミクスも平らにします。まず「標準」、長い区間だけ整えたい場合は滑らかな設定、すでに良好なミックスでは弱い設定から始めます。

### 7.4 グラフ

- 包絡系: 目標値を中心に±20 dBを基本表示
- ゲイン調整系: 0 dBを中心に±20 dBを基本表示
- レベリング前後の包絡差分
- レベリング後－基準カーブ

「レベリング後－基準カーブ」は詳細確認用の固定タイムラインで、標準フローの波形ズーム／高さや再生カーソルとは連動しません。共通部分、基準より下げた部分、上げた部分を色分けします。

### 7.5 処理段試聴

「レベラー前」と「レベラー後」を同じ時刻で切り替えます。これはトーンEQまで適用された信号と、さらにレベラーを通した信号の比較です。「音量を揃えて比較」を使うと、単純な音量差による印象を抑えてレベリングの質を確認できます。

## 8. マスタリング・コンプレッサー

### 8.1 設計目的

このコンプレッサーは、-14 LUFS付近を前提に、ピークだけが高く音の胴体が相対的に細い状態を整えるためのオフライン・マスタリングコンプレッサーです。最終ラウドネスを上げ続けるマキシマイザーではありません。

処理の考え方は次のとおりです。

1. 音楽が実際に鳴っている区間の平均ゲインリダクション目標を指定
2. 曲全体を解析し、その目標になる閾値を自動探索
3. ピークとボディを穏やかに圧縮
4. 圧縮前後のIntegrated LUFSが概ね一致するようメイクアップ
5. 推定True Peak天井を超えそうなメイクアップは制限

このため、コンプ前後を同じラウドネスで比べやすく、「大きく聞こえるから良い」という判断を避けられます。

### 8.2 検出器

- 全チャンネル連動の広帯域パワー
- 30 ms検出窓、10 msホップ
- RMS相当値とブロックピークから作るピークガード
- 低域で過剰に反応しないためのサイドチェインHPF
- 無音／フェード区間を平均目標から除くアクティビティゲート（既定-48 dBFS）

検出値は概念的に「RMSレベル」と「ブロックピーク－6 dB」の高い方を使います。これにより、平均エネルギーだけでなく突発ピークにも反応します。

### 8.3 自動閾値

固定閾値では、マスターごとのレベル差により同じプリセットでも効き方が大きく変わります。EMSはアクティブ区間の平均ゲインリダクションが目標へ近づくよう、閾値を反復探索します。

目標平均GRは「常にその量だけ下げる」という意味ではありません。実際のGRは素材、レシオ、ニー、最大GR、アタック／リリースによって変わり、解析結果に実測平均／最大を表示します。

### 8.4 圧縮カーブと時間応答

- ソフトニー: 閾値付近を滑らかに接続
- レシオ: 閾値より上の圧縮率
- 最大ゲインリダクション: 過圧縮を制限
- アタック: トランジェントをどこまで残すか
- リリース: 圧縮解除の滑らかさ
- 限定的ルックアヘッド: 検出窓とアタックから先読み量を決定

非常に速いアタックと短いリリースは音を前へ出す一方、トランジェント消失や歪みを起こしやすくなります。公開プリセットは平均0.5～1.6 dB、最大1.25～3.5 dB程度の穏やかな範囲です。

### 8.5 LUFS一致メイクアップ

圧縮後のIntegrated LUFSを測り、圧縮前へ戻すための正のメイクアップを計算します。ただし推定True Peak天井を超える場合はメイクアップ量を制限します。

ここで使うTrue Peakは4相の三次補間による推定です。インターサンプルピークへの安全余裕を得る目的ですが、規格準拠メーターの認証値ではありません。

### 8.6 プリセット

| プリセット | 目標平均GR | 最大GR | 性格 |
|---|---:|---:|---|
| ナチュラル | 約0.6 dB | 1.5 dB | アタックと大きな抑揚を優先 |
| 標準 | 約1.0 dB | 2.5 dB | ピークと胴体の基本バランス |
| 密度重視 | 約1.6 dB | 3.5 dB | -14 LUFS内で胴体を前へ |
| 抑揚重視 | 約0.5 dB | 1.25 dB | 遅いアタックでトランジェント優先 |

ユーザープリセットは`.emscompressor`へ保存します。

### 8.7 メトリクスとグラフ

- 自動閾値
- 平均／最大ゲインリダクション
- アクティブ率
- LUFSメイクアップ
- 入力／出力Integrated LUFS参考値
- 入力／出力推定True Peak
- PLR推定値と変化
- 検出音量、閾値、LUFS一致後レベル
- 時間方向のゲインリダクション
- コンプ前後の波形差

検出音量グラフの縦軸はフェードアウトに引っ張られないよう下端を制限しています。グラフが密に見えても、平均／最大GRと差分波形を併せて判断してください。

### 8.8 試聴

- コンプ前
- コンプ後
- 持ち上げ分
- 抑え分

「持ち上げ分」は、時刻ごとのゲインリダクションがLUFS一致メイクアップ量より小さく、最終的にコンプ前より大きくなった正味成分です。「抑え分」は、メイクアップ後もコンプ前より小さくなった正味成分を、聴きやすい正相へ戻したものです。どちらも通常音源より控えめな基準まで安全に自動増幅します。両者の境界は、ゲインリダクション図に表示するLUFS一致メイクアップの横線と対応します。

## 9. マルチバンド・リバーブ

### 9.1 基本構造

リバーブはDry音源をLow / Mid / Highへ分割して再合成する方式ではありません。Dryはそのまま保持し、Wet成分だけを3帯域に分けて生成し、最後に加算します。この構造により、クロスオーバーによる原音の位相変化や定位変化を避けます。

```text
Dry ───────────────────────────────┐
入力 ─ Low Wet ─ IR ─ level/width ─┤
     ├ Mid Wet ─ IR ─ level/width ─┼→ 加算
     └ High Wet ─ IR ─ level/width ─┘
```

### 9.2 空間プロファイル

- Room: 近い初期反射と短い残響
- Studio: 短く密度の高い自然な接着感
- Plate: 滑らかで明るい人工残響
- Hall: 遅れて密度が高まる広い空間
- Stadium: 疎で遅い反射と長い尾
- Church: 石造空間を想定した非常に長い残響

プリセットはマスタリング・スペース、クリエイティブ・リバーブ、特殊空間に分類されます。標準用途は「標準2」「微細な一体感2」等から始めてください。

### 9.3 主な設定

- 効果量、空間サイズ
- Low / Highクロスオーバー
- 初期反射／後期残響
- プリディレイ、ディケイ、ダンピング
- Low / Mid / High別Wet量、幅、Mute / Solo
- ルームシード
- テールを原音尺で切る／自動トリムする設定

リバーブを強くすると最終ピークとIntegrated LUFSも変化します。保存前に最終処理後Bを確認してください。

### 9.4 Wetリボン

リボンの太さは時間ごとのWet/Dry量、色はWet Side Energy比から推定したステレオの広がりを表します。青側が狭く、ピンク側が広い状態です。無音やフェード端の微小値で色が飛ばないようエネルギー下限と時間平滑化を使います。

リバーブタブでは全体に加え、Low / Mid / Highを別々に表示します。高域に実質的なWetエネルギーがない場合は、全幅の疑似リボンを描かず空または細い表示になります。

### 9.5 試聴

- リバーブ前
- リバーブ後
- Wetのみ

WetのみはDryとの差に相当します。聞き取りやすい音量へ持ち上げますが、通常の完成音量と同一ではありません。残響の濁り、過度な高域、定位、テールを診断してください。

## 10. A/B/C比較とスナップショット

### 10.1 共通プレビュー

- 処理前A: 入力音源
- 処理後B: 現在の最終処理結果
- Wetのみ: 最終処理後－処理前の差分診断
- 比較C: 一時保存した別案

停止中にA/B/Wet/Cを押すと、その音源を選んで同じ位置から再生を開始します。再生中は「停止」と表示します。シークバーはクリック／ドラッグした位置へ全候補を同期します。

「音量を揃えて比較」はA/Bの実音量差を抑え、音色やダイナミクスの違いを判断するための機能です。「波形グラフと同期」は共通3波形へ再生位置を表示します。

### 10.2 比較C

「現在のBをCに保存」で、現在の処理設定と処理済み音源を一時保持します。その後設定を変えて再処理すれば、新しいBと以前のCを比較できます。

Cを「今の設定」に呼び出すと、呼び出し前の現在Bを自動的にCへ退避して入れ替えます。これは一段Undoではなく、2つの候補を行き来して追い込むためのピッカーです。ブラウザを閉じると音声スナップショットは失われます。

長尺モードではCを保持しません。メモリ不足を避けるためであり、Cを使う比較は通常長の音源で行ってください。

### 10.3 最初からやり直す

「入力から再処理」はCを消しません。現在の設定を元入力へ適用し直してBを上書きします。すべての設定、音源、Cを消す操作とは別です。

## 11. 保存と正規化

### 11.1 処理WAV

最終処理後Bを選択したWAV形式で保存します。処理段のピークを自動的に目標へ合わせるとは限りません。

### 11.2 ピーク正規化WAV

最終処理後のサンプルピークを指定dBFSより0.3 dB低い内部上限へ合わせます。インターサンプル・ピークへの安全余裕を作るためで、Integrated LUFSは曲ごとに揃いません。

### 11.3 ラウドネス正規化WAV

Integrated LUFS参考値から目標ゲインを計算します。ピークが正規化保存ピークへ達する場合は、0.3 dB低い内部上限でゲインを制限し、目標LUFSへ届かないことがあります。目標LUFSが低く、この上限へ達しない場合は安全マージンによる追加減衰を行いません。これは異常ではなく、リミッターなしでピーク安全を優先した結果です。

目標-14 LUFSへ届かない場合は次を確認します。

- サブソニックが余裕を消費していないか
- レベラーの局所ピークが残りすぎていないか
- コンプレッサーの平均／最大GRが小さすぎないか
- リバーブWetが新しいピークを作っていないか
- 外部リミッターが本当に必要な用途か

### 11.4 ディザーとTrue Peak

「ログ・全体設定」タブの出力設定では、16-bit PCMディザーをON/OFFできます。既定はONです。TPDF（2つの一様乱数の差）を、16-bit量子化の直前に最大1 LSBの範囲で加えます。専用のディザーシードを同じ値にすれば、同一の入力・設定から同じディザー波形を再現できます。バッチ処理では開始時のON/OFFとシードを固定します。

アプリ全体のピーク安全は主にサンプルピークで扱い、正規化保存で指定ピークへ達する場合だけ0.3 dBの安全余裕を加えます。コンプレッサー段だけ推定True Peakをメイクアップ制限に使います。いずれも認証済みのTrue Peak保証ではありません。ディザーはTrue Peakの制御やリミッターの代わりにはなりません。

## 12. バッチ処理

### 12.1 基本

バッチ開始時に現在の処理段設定をスナップショットし、全ファイルへ同じ設定を順次適用します。処理中に画面設定を変えても、そのバッチの途中結果へは混ざりません。

1. 標準フロー／詳細タブで設定を決める
2. バッチ処理でトーンEQ、レベラー、コンプレッサー、リバーブのON/OFFを確認
3. ファイル群を選ぶ
4. 保存方法、形式、接尾辞、正規化を選ぶ
5. 保存先フォルダを選び、開始

### 12.2 安定性優先

バッチ中は共通波形を能動的に更新しません。大量のCanvas描画と試聴用WAV生成を避け、DSPと保存を優先します。バッチ開始時には標準フローの入力、処理結果、C、試聴キャッシュを解放します。標準フローのBを残したい場合は、先にWAVとして保存してください。

### 12.3 要確認ステータス

右ペインには、自動処理で見逃すべきでない項目を表示します。

- ラウドネス目標未達
- ピーク制限によるゲイン制約
- PCMクリップの可能性
- 測定値欠落
- ファイル処理エラー

処理完了だけでなく、この一覧が空か、意図した制約だけかを確認してください。

### 12.4 CSV

バッチレポートCSVには、開始時設定の一覧に続いて、各ファイルを1行ずつ記録します。入力／出力時間と形式に加え、トーンEQ（モデル、Residual、サブソニック処理、手動Bell）、レベラー、コンプレッサー（平均／最大GR、メイクアップ、PLR）、リバーブ、各段のLUFS参考値・ピーク、保存ゲイン、PCMクリップ、出力形式・サイズを追えます。CSVは監査補助であり、規格準拠測定証明ではありません。

## 13. ユーザープリセットと専用拡張子

| 処理段／用途 | 拡張子 | 保存内容 |
|---|---|---|
| レベラー | `.emsleveler` | レベラーのユーザープリセット群 |
| トーンEQ | `.emstoneeq` | 補正オプションのユーザープリセット群。モデル選択は含まない |
| コンプレッサー | `.emscompressor` | コンプレッサーのユーザープリセット群 |
| リバーブ | `.emsreverb` | リバーブのユーザープリセット群 |
| リファレンスモデル | `.emsreference` | 目標スペクトル、IQR、解析条件 |

内部は可読なJSONで、`format`、`fileType`、`schemaVersion`を持ちます。読み込み時に処理段を検証し、たとえばリバーブ欄へコンプレッサーファイルを読み込む操作を拒否します。

専用拡張子により、OSの「開く」ダイアログでは対象ファイルだけを絞り込めます。開発中の旧JSONを読み込む必要がある場合だけ「すべてのファイル」を選べますが、公開版は専用拡張子を使用してください。

ブラウザ内の「追加」は現在設定をローカルのユーザープリセットへ登録し、「プリセット保存」はそのライブラリをファイルへ書き出します。ブラウザデータを消す前に必要なライブラリを保存してください。

## 14. 推奨ワークフロー

### 14.1 万人向けの開始点

1. トーンEQ: ON、リファレンスなし、補正オプション標準
2. レベラー: ON、標準
3. コンプレッサー: ON、標準
4. リバーブ: ON、標準2
5. 入力から処理
6. A/Bを音量一致で確認
7. 各処理段前後を確認
8. 必要なら一つずつ変更して再処理

モデル寄せが必要か不明な場合は、リファレンスなしのままにします。これでもサブソニック整理と任意の手動Bellは利用できます。

### 14.2 Sunoの低域余裕が問題の場合

1. トーンEQのラウドネス効率化EQをON
2. 自動カットオフ結果を確認
3. 補正前後スペクトルの20～100 Hzを見る
4. レベラー後のピーク余裕を確認
5. コンプレッサー標準で平均約1 dBのGRを試す
6. -14 LUFS正規化の未達／ピーク制限を確認

### 14.3 高域過剰が問題の場合

まず適切なリファレンスとResidualで広域／局所過剰を確認します。それでも10 kHz付近が刺さる場合、既定OFFのBell 2（10 kHz、-3 dB、Q=2）をONにし、周波数とQを音源へ合わせます。自動補正と手動Bellを同じ帯域へ過剰に重ねないでください。

### 14.4 比較Cの使い方

1. 標準設定で処理してBをCへ保存
2. トーンEQモデルまたはコンプ強度を一つだけ変更
3. 入力から再処理して新しいBを作る
4. B/Cを同じ位置、音量一致で比較
5. Cが良ければ呼び出して入れ替える

複数要素を同時に変えると原因が分かりません。1回の比較では1つの処理段か1つの主要パラメータだけを変えます。

## 15. 制限事項

- 標準PCM／32-bit float以外のWAVE_FORMAT_EXTENSIBLEサブ形式、RF64、64-bit Float WAV、圧縮WAVは正式対応外
- 4 GBを超えるRIFF/WAVは扱えない可能性がある
- WAVのタグ、BWF、カバー画像等のメタデータは保持しない
- 16-bit PCMディザーは量子化歪みを軽減するもので、ノイズシェーピングではない
- Integrated LUFSは参考実装
- コンプレッサーのTrue Peakは4相補間による推定
- ブリックウォール・リミッター／マキシマイザーなし
- 多チャンネルのスピーカーレイアウト情報を保持しない
- ブラウザのMP3デコード結果は実装依存
- WMAなど、ブラウザが標準でデコードできない音声形式は対応外
- 40 kHz未満では、リファレンスモデルを使うTonal Envelope補正とResidual Correction EQは使用できない
- 長いリバーブテールでは出力時間とメモリが増える
- 長尺モードでは75秒試聴窓を使い、比較Cと処理段前後試聴は使用できない
- ユーザープリセットはブラウザ単位。プライベートモードやサイトデータ削除で消える
- 単一HTMLのためDSP、UI、モデルデータを一つのファイルへ収録しており、初回読み込みとメモリ負荷が大きい

## 16. トラブルシューティング

### 処理が重い

- 他の重いタブやアプリを閉じる
- 比較Cを不要ならクリアする
- 長いリバーブテールを避ける
- バッチのファイル数を分ける
- 44.1 / 48 kHzのステレオWAVで確認する

### 設定を変えたのに音が変わらない

設定変更は予測グラフへ反映されても、実音声へ自動適用しません。「入力から再処理」を押してください。

### リファレンスモデルを選んでも操作がグレーのまま

処理中は安全ロックされます。処理終了後にモデルを選び直してください。「モデルなし」ではTonal EnvelopeとResidualが仕様として無効です。

### -14 LUFSへ届かない

ピーク余裕が不足し、正規化ゲインが制限された可能性があります。ラウドネス効率化EQ、レベラーの飛び出し、コンプレッサーのGR、リバーブ後ピークを順番に確認してください。

### プリセットが開くダイアログに見えない

読み込む処理段と拡張子が一致しているか確認します。旧JSONを確認する場合だけファイル種別を「すべてのファイル」へ変えます。

### グラフと外部アナライザーが一致しない

窓長、平滑化、チャンネル集約、正規化、True Peak方式が異なるため完全一致しません。EMSのグラフは処理前後の相対比較に使い、最終納品値は目的に合う外部メーターで確認してください。

## 17. 公開版1.0.0の位置づけ

v1.0.0は、旧MELの完成したレベラーとリバーブに、TEMの高解像度EQ、ユーザー・リファレンス、適応型コンプレッサー、統合UI、比較C、4段バッチを加えた最初の公開版です。

公開前には、代表的なSuno音源、一般音源、FLAC、モノラル、長尺音源で単曲／バッチ、再処理、保存を確認し、外部LUFS／True Peakメーターを併用して正規化出力を検証しました。今後DSP、正規化、保存、メモリ管理を変更する場合は、同じ観点の回帰確認を行ってください。
