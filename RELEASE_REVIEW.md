# Envelope Mastering Suite v1.0.0 Release Review

## 判定

**公開版1.0.0としてリリース可能。**

旧Music Envelope Leveler v2.5.0のレベラー／3帯域リバーブを基準に、トーンEQ、ユーザー・リファレンスモデル、適応型マスタリングコンプレッサー、統合UI、A/B/C比較、4段バッチを実装した。主要機能は揃い、単曲処理とバッチ処理の実運用確認が完走している。

代表的なSuno音源、一般音源、クラシック系FLAC、モノラル、長尺音源での実運用、外部LUFS／True Peak計測、単曲／バッチ、再処理、保存を確認した。長尺時のメモリ制約には専用の縮退動作を設け、公開対象として重大なDSP不良、保存破損、状態遷移不良は確認されていない。

## リリース対象

- 製品名: Envelope Mastering Suite
- 略称: EMS
- バージョン: 1.0.0
- 実装形態: ローカル動作の単一HTMLアプリ
- 主対象: Suno等のAI生成音源、完成済み2chミックス
- 中核機能: エンベロープ・レベリング

## 収録機能

### 4段DSP

1. 高解像度トーンEQ
2. エンベロープ・レベラー
3. 適応型マスタリングコンプレッサー
4. Low / Mid / High Wet-onlyリバーブ

全段を独立してON/OFFでき、単曲とバッチで同じ順序を使用する。

### トーンEQ

- 20 Hz～20 kHz、1/24オクターブ解析
- リファレンスモデルと補正オプションの分離
- 制約付き最小位相FIR
- Residual Correction EQ
- ラウドネス効率化EQ
- 2バンド手動Bell EQ
- 「リファレンスなし」を安全な既定値として採用
- 3曲以上からのユーザー・リファレンスモデル作成／保存／読込

### コンプレッサー

- 平均GR目標に基づく曲別自動閾値
- ピークガード、サイドチェインHPF、ソフトニー、最大GR、ルックアヘッド
- Integrated LUFS一致メイクアップ
- 推定True Peak天井
- 前後／差分試聴と詳細メトリクス

### 運用機能

- 共通3波形とA/B/Wet/C試聴
- B/Cを入れ替えられる比較スナップショット
- 処理段前後の独立試聴
- 4段ユーザープリセット
- 用途別専用拡張子とファイル種別検証
- 4段バッチ、波形描画停止、要確認ステータス、CSV
- 日英UI

## 実装上の安全策

- 処理は毎回元入力から再構成し、意図しない重ね掛けを防止。
- 処理中は全処理段の設定をロック。
- トーンEQのブースト／カット、傾斜、特徴幅を制約。
- レベラーの時間ゲインに量、速度、最大値の制約。
- コンプレッサーに最大GRと推定True Peakメイクアップ制限。
- Dryを帯域分割せず、リバーブWetだけを3帯域化。
- バッチ開始時に設定を固定し、処理中のUI変更を分離。
- バッチ開始時に標準フローの入力、B/C、試聴キャッシュを解放し、長尺連続運用のメモリ競合を回避。
- 正規化保存で指定ピークへ達する場合は0.3 dB低い内部上限を使い、インターサンプル・ピークへの余裕を持たせる。
- 異なる用途のプリセットファイルを`fileType`で拒否。

## 完了した公開前ゲート

### 音声回帰

- 同一設定による単曲とバッチを実運用し、4段の処理・保存・CSVが完走。
- 44.1 kHz／48 kHz、8／16／24／32-bit PCM、MP3、FLAC、モノラル、長尺を確認。
- 長尺でリバーブ再処理、リボン表示、ラウドネス正規化WAV保存まで完走。

### 外部計測

- 複数のSuno音源とクラシック系FLACで外部Integrated LUFS／True Peakを測定。
- ラウドネス正規化出力は外部計測で概ね-13.9 LUFS付近を維持し、以前True Peakが高めだったケースも安全側へ改善した。
- サブソニック整理、ピーク制限、LUFS未達の表示とCSVを確認。

### UI／ファイル

- 日英UI、A/B/Wet/C、停止、シーク、詳細タブ試聴、初回起動とモデルなし→ありの状態遷移を確認。
- 5種類の専用ファイルを保存→再読込し、異種ファイルの拒否を確認。

### バッチ

- 10曲規模の連続処理を複数回完走し、対応外入力のエラーもレポートへ明示。
- 波形描画停止中の進捗、警告、正規化、上書き回避、接尾辞、保存形式、CSVを確認。
- 標準フロー長尺処理後に長尺バッチを実行するケースも確認。

## 既知の制約

- Integrated LUFSは参考実装で、規格納品の認証値ではない。
- True Peakはコンプレッサー段の4相補間推定で、認証済み測定ではない。
- マキシマイザー／ブリックウォール・リミッターなし。
- 16-bit PCMのTPDFディザーはON/OFF可能で、ノイズシェーピングは行わない。
- WAVメタデータ、標準PCM／32-bit float以外のWAVE_FORMAT_EXTENSIBLEサブ形式、RF64等は正式対応外。
- WMAなどブラウザ標準でデコードできない形式は正式対応外。FLACはブラウザがデコードできる環境で利用できる。
- リファレンスモデルを使うTonal Envelope補正／Residual Correction EQは40 kHz以上の入力を必要とする。
- 曲全体をオンメモリで処理するため、長時間／多チャンネル／長いテールはメモリを消費する。大きなPCM音源は75秒試聴窓へ縮退し、比較Cと処理段前後試聴を無効化する。
- ユーザープリセットとモデルのブラウザ内保存は、サイトデータ削除で失われる。

## リリース後の変更方針

DSP、正規化、保存、メモリ管理、リバーブ表示を変更する場合は、通常長と長尺の単曲／バッチ、再処理、保存、外部LUFS／True Peak計測を再確認する。変更内容は`CHANGELOG.md`と`USER_GUIDE.md`へ同時に反映する。

旧`STABILITY_REVIEW.md`、`PHASE*_REPORT.md`、`REVERB_PRESET_NORMALIZATION_AUDIT.md`はMEL時代の履歴資料であり、現行リリース判定は本書を正とする。
