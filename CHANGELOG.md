# Envelope Mastering Suite Changelog

## v1.1.0

### 公開判断

- v1.1.0として公開。コード／自動回帰と問題Suno音源・無ノイズのイントロドラム試料によるプリセット校正を完了した。
- 公開優先の判断により、デノイズを含む網羅的な最終実働レビューは本版では省略した。これは既知の検証ギャップとして`RELEASE_REVIEW.md`へ記録し、次のDSP／保存／正規化／メモリ管理変更前の必須回帰とする。

### 適応型デノイズ

- 5段処理チェーンの先頭へ、適応型デノイズを追加。段内順序はDe-click → Micro-burst Repair → Spectral Denoiseで固定。公開既定値はON／「弱い」。
- 20 msロバスト統計と複数の保護特徴から微小クリック候補を評価し、48 kHz換算16サンプル以下の高Confidence候補だけを制約付きHermite補間で修復。
- 数ms～数十msのSuno固有プチノイズ群を扱うMicro-burst Repairを追加。長尺対応のブロック検出で局所ピーク突出、クレスト、極性偏りを評価し、突出した高周波残差だけを滑らかに局所抑制。
- 検出したクリックをアンカーに高域残差の裾野を前後へ追跡するBurst Tail Repairを追加。「強い」では前後各60 ms、「非常に強い」では各120 msまで処理し、長い広帯域トランジェントを自動修復から除外して音楽成分を維持。
- アンカー周辺だけを512点STFTで処理する局所スペクトル修復を追加。前後の局所参照より過剰な高域成分を抑え、時間領域の残差抑制だけでは残ったレコード傷状の「ジリジリ」を処理。全曲Spectral Denoiseの最大3 dBは増やさない。
- 強度プリセットを「弱い」「普通」「強い」「非常に強い」の4段階へ整理。実音源の無ノイズ・ドラム区間とSunoプチノイズ区間を対で計測し、弱いは実質無変化、普通は最大4.5 dB、強いは最大10 dB、非常に強いは低Confidence候補まで最大12 dB処理するよう校正。
- Micro-burstのトランジェント保護を、局所エネルギーの立ち上がりに応じてクリック本体と裾野の両方へ連続適用する方式へ強化。調整範囲を200%まで拡張し、低Confidence検出を増やしてもスネアやハイハットを優先的に保護できるよう変更。
- 従来の「標準」を「普通」へ改称し、さらに保守的な「弱い」を追加。「弱い」は無ノイズのドラム試料を実質無変化に保ちながら、ノイズ試料では最低1か所以上を処理する境界へ校正。
- 強弱とは別に「クリック特化」「傷ノイズ・裾野特化」「持続高域ノイズ特化」の目的別プリセットを追加。各方式で不要な処理器は明示的にOFFにし、対象外の音質変化を抑制。
- 低Confidence、長い候補、安全検査不合格、件数上限到達を自動修復せず要確認として記録。
- 曲全体を疎に解析し、平坦性、持続性、推定ノイズ床、非調波性から高域アーティファクトを評価。確度不足時はBYPASSし、必要な区間・帯域だけを最大3 dB処理。
- トーナル成分とトランジェントを保護し、L/R共通マスク、時間／周波数平滑化、チャンク処理でステレオ像と長尺安定性を優先。
- 32 kHz未満ではSpectral処理だけをBYPASSし、De-clickと後段処理を継続。

### レビュー、試聴、運用

- 標準フロー概要とデノイズ詳細タブへ、候補タイムライン、帯域リスク、減衰時間変化、処理メトリクス、要確認警告を追加。
- デノイズ前／後、De-click差分、Micro-burst除去、Spectral除去、全差分の処理段試聴を追加。差分ゲインとピークを安全制限し、長尺では75秒窓へ縮退。
- `.emsdenoise`ユーザープリセット、標準フロー／バッチON/OFF連動、処理中ロック、バッチ警告カウンター、ファイル別CSV監査項目を追加。
- デノイズ詳細タブへ、De-click確度、Micro-burst／裾野追跡感度、最大追跡時間、クリック／裾野最大抑制、トランジェント保護、局所スペクトル修復のON/OFF・感度・開始周波数・最大抑制、全曲Spectral最大抑制の調整UIを追加。変更値はカスタム状態とユーザープリセットへ保存。
- デノイズ詳細の3グラフへ凡例と小数第1位の軸目盛を追加。広帯域を3区分で評価するアーティファクト帯域スコアは、低解像度スペクトラムに見えないよう折れ線から帯域別棒グラフへ変更。
- De-click／Micro-burst候補タイムラインへ曲中時刻の目盛と、デノイズ処理段試聴に連動する再生位置カーソルを追加。凡例はノイズの正解分類ではなく、自動修復を実行／見送った結果であることが明確になる文言へ変更。
- Micro-burst Repairの毎分修復件数制限を廃止。高密度な音源でも先着順で後続候補を見送らず、候補ごとの安全検査とファイル上限だけを適用するよう変更。
- デノイズの段別差分試聴を再計算による推定から実処理の中間出力差分へ変更。De-click／Micro-burst／Spectralの除去音が別段へ混入しないようにし、長尺音源では追加PCM保持を避けるため個別差分を無効化して前後・全差分試聴へ安全に縮退。
- デノイズ処理段を試聴中にボタンまたはShift+Gで再処理した場合も、全試聴経路を停止してから処理を開始するよう修正。
- De-clickは約0.33 ms以下の単発PCMスパイク／編集点クリックが対象であることと、「強い」は打楽器や高域質感を損なう可能性のある緊急救済設定であることをUIとガイドへ明記。
- デノイズの高リスク帯域をトーンEQ最終カーブが2 dB以上持ち上げる場合、TEM再ブースト警告を表示。TEMカーブとプリセットは自動変更しない。
- 製品表記、処理段数、README、USER GUIDE、RELEASE REVIEWをv1.1.0へ更新。

## v1.0.0

公開版。`v1.0.0-rc.1`から、実音源の単曲／バッチ／長尺回帰と外部ラウドネス計測を完了し、公開運用向けの安定性と安全余裕を整えた。

### 長尺・メモリ管理

- 大きなPCM音源では、内部プレビューWAVの代わりに現在位置から75秒の試聴窓を使用。比較Cと処理段前後試聴は無効化し、処理・保存を優先。
- 長尺のWAV保存と正規化をチャンク化し、正規化用の全曲コピーを追加で作らない方式へ変更。
- 標準フローからバッチを始める際、標準フローの入力、B/C、試聴キャッシュを解放してから処理を開始。長尺の連続運用時のメモリ競合を防止。

### 出力安全性・表示

- ピーク／ラウドネス正規化で指定ピークへ達する場合、内部サンプル上限を「正規化保存ピーク − 0.3 dB」へ制限し、インターサンプル・ピークへ安全余裕を追加。True Peakの規格保証ではない。
- モノラル音源を含む3帯域Wetリボンの高域表示を、元サンプルレートで連続観測する軽量な方式へ変更。無音帯域を誤って矩形表示しない。
- `Shift + G`の補正実行ショートカットは、プルダウン、スライダー、チェックボックスにフォーカスがある場合も利用可能にした。テキスト入力中とバッチ処理中は無効。

### 対応形式・検証

- ブラウザがデコードできるFLAC入力を確認。WMAなどブラウザ標準でデコードできない形式は正式対応外。
- リファレンスモデルを使うTonal Envelope補正／Residual Correction EQは40 kHz以上の入力を必要とすることを明文化。40 kHz未満は「リファレンスモデルなし」でラウドネス効率化EQと手動Bell EQを利用可能。
- Suno音源、クラシック系FLAC、モノラル、44.1 / 48 kHz、各PCMビット深度、長尺、リバーブ再処理、単曲／バッチ、ラウドネス正規化保存を回帰確認。

## v1.0.0-rc.1

公開版1.0.0へ向けた最初のリリース候補。Music Envelope Leveler（MEL）v2.5.0を基礎に、Tonal Envelope Matcher（TEM）と新方式のマスタリングコンプレッサーを統合し、製品名をEnvelope Mastering Suiteへ変更した。

### 追加改善

- `Shift + G`で、バッチ処理タブを除く任意のタブから補正処理を起動可能にした。
- 手動Bell EQの説明を専用の内容へ修正し、コンプレッサーのゲインリダクション図にLUFS一致メイクアップの基準線を追加。
- コンプの前後差分試聴を、メイクアップ後に正味で増えた「持ち上げ分」と、正味で減った「抑え分」へ分離。
- リバーブ段の試聴へWetのみを追加。最終段からリバーブ入力を差し引いた実際のWet成分を確認できる。
- 正規化、ラウドネス基準、出力形式、16-bit PCMディザーを「ログ・全体設定」タブへ集約。
- 16-bit PCM書き出しへ、既定ON・再現可能シードのTPDFディザーを追加。
- バッチCSVを処理段別の監査レポートへ更新し、トーンEQ、レベラー、コンプレッサー、リバーブ、保存結果をファイルごとに記録。

### 統合アーキテクチャ

- 処理順を「トーンEQ → レベラー → コンプレッサー → 3帯域リバーブ」に統一。
- 各処理段を独立してON/OFFでき、設定変更後は元入力から再処理する方式へ整理。
- 標準フローを、ファイル選択、4段プリセット、処理、A/B/C試聴、保存まで1画面で完結する構成へ再設計。
- 右ペイン上部へ補正前、最終処理後、差分の共通3波形を配置し、タブ別の詳細レビューを下部へ集約。
- バッチ処理中は波形描画を停止し、DSPと保存の安定性を優先。

### トーンEQ

- 20 Hz～20 kHz、1/24オクターブ解像度の高解像度解析をTEMから移植。
- 制約付きTonal Envelope補正を最小位相FIRとして実装。
- 局所的な残差を扱うResidual Correction EQ、サブソニックを整理するラウドネス効率化EQ、2バンド手動Bell EQを追加。
- リファレンスモデル「なし」を既定化。「なし」ではモデル寄せとResidualを強制OFFにし、効率化EQと手動Bellだけを利用可能にした。
- リファレンスモデルと補正オプションを別々に選択・保存する仕様へ統一。
- 3曲以上の統計中央値とIQRからユーザー・リファレンスモデルを作成する機能を追加。
- 手動Bell編集中は重い音声処理を行わず、予測グラフだけを更新するよう最適化。

### エンベロープ・レベラー

- 旧MELの中核DSPを統合チェーンへ移植。
- 包絡系とゲイン系グラフを分離し、レベリング前後差分、レベリング後－基準カーブを詳細タブへ追加。
- 詳細グラフを共通波形のズーム／高さ／再生カーソルから独立。

### マスタリング・コンプレッサー

- 旧試作方式を廃止し、平均ゲインリダクション目標から曲ごとの閾値を自動探索する方式へ全面置換。
- 連動広帯域検出、サイドチェインHPF、ピークガード、アクティビティゲート、ソフトニー、最大GR、アタック／リリース、限定的ルックアヘッドを実装。
- 圧縮前後のIntegrated LUFSを一致させるメイクアップと推定True Peak天井を実装。
- ナチュラル、標準、密度重視、抑揚重視のシステムプリセットを追加。
- 自動閾値、平均／最大GR、アクティブ率、LUFS、推定True Peak、PLR、前後差分を表示。
- コンプ前／後／持ち上げ分／抑え分の処理段試聴を追加。

### リバーブ、比較、プリセット、バッチ

- Dryを維持しLow / Mid / HighのWetだけを帯域処理する旧MELの3帯域リバーブを継承し、帯域別Wetリボンを追加。
- 処理前A、処理後B、Wetのみ、比較Cの共通タイムライン試聴と、設定＋音源の比較スナップショットを実装。
- 4処理段のユーザープリセットUIを統一。
- 専用拡張子`.emsleveler`、`.emstoneeq`、`.emscompressor`、`.emsreverb`、`.emsreference`を導入し、異なる処理段の誤読込を拒否。
- バッチ設定へトーンEQとコンプレッサーを追加し、LUFS未達、ピーク制限、PCMクリップ、測定欠落、エラーを右ペインへ表示。

### 文書と製品表記

- 画面、設定値リスト、バッチCSVの製品名をEnvelope Mastering Suiteへ統一。
- README、USER GUIDE、RELEASE REVIEWを4段DSPの現仕様で全面更新。
- 旧MELの変更履歴は移行前履歴として以下に保持。

---

## Music Envelope Leveler 移行前履歴

## v2.5.0

- v2.5.0-rc.1を利用者が一通り実運用し、異常が認められなかったため正式版へ昇格。rc.1からDSP、UI、プリセット値の変更なし。
- カスタム・リバーブプリセットピッカーをプリセット制御へ直接接続し、旧selectは同期用ミラーへ縮小。
- A/B/Wet試聴の旧世代関数を削除し、Wet対応実装を単一定義化。
- `previewController`を追加し、再生・停止・モード切替・シークの窓口を統合。
- `processAudioPipeline()`を追加し、標準フローとバッチ処理でレベリング、リバーブ、LUFS計測を共通化。
- 旧`previewCurrentCurve`と`processOneBatchFile`の重複実装を削除。
- DSP式、14プリセット、正規化、出力形式は変更なし。


## v2.4.2

- Phase 1構造整理を正式採用。DSPアルゴリズムと処理順はv2.4.1から変更していない。
- `APP_VERSION`を追加し、画面、設定値リスト、バッチCSVのバージョン表記を一元化。
- 分散していた英訳辞書を単一の`I18N_EN`へ統合。
- `applyLanguage`、`updateLabels`、`updatePreviewModeUi`、`updateBatchReverbInfo`、`setStatus`、`setProgress`を単一定義へ平坦化。
- `setProcessingUi`、`applyGlobalControlLocks`、`updateBatchButtons`を単一定義へ平坦化。
- `resetAll`を単一定義へ統合し、初期化処理を`initializeApp()`へ明示化。
- 明確に未使用だった旧メディア要素同期初期化コードを削除。
- 内蔵14リバーブプリセットの「効果量」初期値をすべて100%へ統一。
- 従来100%以外だった9プリセットは、旧`帯域Wet × 効果量`とほぼ同じ実効Wet量になるよう、倍率をLow / Mid / High Wetへ吸収。
- 帯域WetはUIの0.1%刻みに合わせて丸めたため、旧版との差は各帯域で最大0.05ポイント。
- ユーザープリセットは変換せず、保存された効果量をそのまま維持。
- 合成音源でPhase 1平坦化によるDSP差がないことを確認。利用者が実音源で標準フロー処理とバッチ処理を確認し、従来との動作差なしと判定。


## v2.4.1

- カスタム・プリセットピッカーの高さを、トリガー上下の空き領域と候補一覧の自然高から動的に決定。
- 下側が狭い場合は上向きに開き、十分な空間がある場合は14候補をスクロールなしで表示。
- 全候補が収まらない場合、左側の候補一覧だけを独立スクロール領域に変更。
- マウスホイール操作、視認性のあるネイティブスクロールバー、キーボード移動時の自動追従に対応。
- 候補行、カテゴリ見出し、グループ間余白を圧縮し、標準プルダウンに近い表示密度へ調整。
- ピッカー内部のスクロールで位置再計算が走り、スクロール位置が先頭へ戻る問題を防止。
- 14システムプリセット、DSP、IR、マクロ、Wetのみ試聴、LUFS、正規化、バッチ処理は変更していない。

## v2.4.0

- 空間・リバーブプリセット欄を、説明プレビュー付きのカスタム・プリセットピッカーへ変更。
- 候補へのマウスオーバーまたはキーボード移動で、適用前にカテゴリ、名称、短い用途説明を表示。
- 候補移動だけでは設定を変更せず、クリックまたはEnterで適用、Escでキャンセルする操作を実装。
- 現在選択、ユーザープリセット、カスタム未保存状態、日本語／英語表示、処理中ロックを既存プリセット機構と同期。
- 第三カテゴリ`特殊空間 / Experimental Space`を追加。
- 特殊空間へ`密室 / Close Chamber`と`背後の大空間 / Vast Rear Space`を追加。
- 既存12システムプリセットの設定値、DSP、IRプロファイル、効果量・空間サイズ、Wetのみ試聴、LUFS、正規化、バッチ処理は変更していない。

## v2.3.0

- システムの空間・リバーブプリセットを、`マスタリング・スペース`と`クリエイティブ・リバーブ`の2カテゴリへ分類。
- プリセット選択欄を`optgroup`表示に変更し、ユーザープリセットも独立グループへ表示。
- 選択中プリセットの説明欄へ用途カテゴリを明示。
- マスタリング・スペースへ`マイクロアンビエンス`、`奥行き`、`センター保持ワイド`、`暖かい空間`の4プリセットを追加。
- 既存8プリセットの設定値、DSP、IR生成、効果量・空間サイズマクロ、Wetのみ試聴、LUFS、正規化、バッチ処理は変更していない。

## v2.2.3

- `Wetのみ`試聴時の案内文へ、A/Bの音量揃えを行わないことを明記。
- 日本語表示を「Wet成分を実際の加算音量で再生しています。保存音源には影響しません。音量揃えも行いません。」へ変更。
- 英語表示へ`Level matching is not applied.`を追加。
- 音声処理、Wet差分再生方式、UI配置、プリセット、LUFS、正規化、バッチ処理は変更していない。

## v2.2.2

- プレビューの`Wetのみ W / Wet Only W`表記を`Wetのみ / Wet Only`へ簡略化。
- 右ペインの幅を削らず、アプリ全体の最大幅を1280pxから1400pxへ拡張。
- 左ペインを390pxから470pxへ拡張し、`再生`、`処理前 A`、`処理後 B`、`Wetのみ`を横一列に配置。
- 1100px以下では1カラムへ移行し、520px以下ではプレビューボタンの折返しを許可。
- DSP、Wet差分再生、プリセット、LUFS、正規化、波形、バッチ処理は変更していない。

## v2.2.1

- `空間サイズ`下の3ラベルを均等3列配置へ変更し、中央の`標準`をスライダー中央へ正確に整列。
- `スタジアム`プリセットの`効果量`を90%から100%へ戻し、Low / Mid / High Wetを`8 / 25 / 34%`から`7.2 / 22.5 / 30.6%`へ変更。実効Wet量はv2.2.0と完全に同じため、音響出力は変わらない。
- A/Bプレビューへ`Wetのみ W / Wet Only W`モードを追加。最終処理後信号とリバーブ入力信号を同期再生し、後者を逆相加算することで、実際に加算されているリバーブWet成分だけを試聴可能にした。
- Wetのみ試聴は保存音源、正規化、バッチ処理へ影響しない。Wetモード中はA/B音量一致を無効化し、実際のミックス音量で再生する。
- Wetモード用バッファは必要時だけ生成し、通常A/B用Rawバッファと同時保持しないことで長尺音源の追加メモリ使用を抑制。
- リバーブが未適用、Wet 0%、全帯域Mute等で最終音源とリバーブ入力音源が同一の場合、Wetのみボタンを無効化。
- DSP、IR生成、LUFS、正規化、WAV保存、バッチ処理、右ペイン波形は変更していない。

## v2.2.0

- 標準フローの`設定を開閉`最上部へ`リバーブ設定`セクションを追加。
- 従来の`全体Wet倍率`を、同じDSPパラメータを使う`効果量 / Effect Amount`へ改称して移動。
- `空間サイズ / Space`スライダーを追加。0=コンパクト、100=プリセット基準、200=広大。
- 空間サイズに連動してLow / Mid / Highのプリディレイ、ディケイ、ステレオ幅、および共通の初期反射量・後期残響量を詳細設定欄へ実値として反映。
- 空間サイズは基準値から毎回再計算し、往復操作による累積誤差を防止。100へ戻すと基準値へ完全復元。
- 詳細リバーブ設定を直接変更した場合、その状態を新しい基準として空間サイズを100へリセット。
- ユーザープリセットにはマクロ計算後の詳細設定値を保存し、読込時は同じ音を空間サイズ100の基準として復元。
- 効果量・空間サイズの変更では音声を自動再処理せず、既存どおりdirty状態にして明示的な適用を要求。
- バッチ設定要約へ効果量と空間サイズを表示。DSP、IR生成、プリセット値、正規化、波形処理は変更なし。

## v2.1.2

- 右ペイン2段目の波形を、リバーブ前の`state.processed`から、リバーブを含む最終音源`state.finalProcessed`へ変更。
- 3段目の差分波形を、`state.processed − state.raw`から`state.finalProcessed − state.raw`へ変更。
- 4段目は従来どおりリバーブを含めず、`state.processed − state.referenceAudio`を表示。名称を「（参考）レベリング後 − 基準カーブ」へ変更。
- リバーブテールで最終音源が長くなる場合、4段の時間軸を最終処理後音源へ揃え、入力終了後を無音として追加テールも表示・比較できるようにした。
- 波形カーソルも共通の最終処理時間軸へ同期。
- リバーブプリセット、DSP、LUFS、正規化、バッチ処理、WAV入出力は変更していない。

## v2.1.1

- 実音源によるv2.1.0プリセット試聴結果を反映。
- 「標準2」「微細な一体感2」「ボーカルの空気感2」「広がり2」「大聖堂」は変更なし。
- 「ライブハウス2」の帯域Wetと全体Wetを下げ、Late量、プリディレイ、ディケイ、ステレオ幅を短くタイトな小会場向けに再調整。
- 「ホール2」のLow / Mid / High Wetをv2.1.0比でそれぞれ30%低減。ディケイ、初期／後期残響比、空間幅は維持。
- 「スタジアム」の全体Wetを100%から90%へ変更し、効果量を10%低減。その他の設定とStadiumプロファイルは維持。
- DSP、空間プロファイル生成式、補正、正規化、状態管理、バッチ処理は変更していない。

## v2.1.0

- リバーブシステムプリセットを、6種類の空間プロファイルを用途別に使い分ける8プリセット構成へ更新。
- 旧「標準」を削除し、Studioプロファイルの「標準2」を追加。
- 旧「微細な一体感」を削除し、Studioプロファイルの「微細な一体感2」を追加。
- 旧「ボーカルの空気感」を削除し、Plateプロファイルの「ボーカルの空気感2」を追加。
- 旧「広がり」を削除し、Hallプロファイルの「広がり2」を追加。
- 旧「ライブハウス」を削除し、Roomプロファイルの「ライブハウス2」を追加。
- 旧「ホール」を削除し、Hallプロファイルの「ホール2」を追加。
- Churchプロファイルを使う演出用プリセット「大聖堂」を追加。
- 「スタジアム」はStadiumプロファイルと従来の全設定値を維持。
- 全プリセットの日本語／英語名と用途説明を更新。
- Room / Studio / Plate / Hall / Stadium / Churchの全プロファイルに最低1つのシステムプリセットを割り当て。
- DSP、空間プロファイル生成式、補正処理、正規化、状態管理、バッチ処理は変更していない。

## v2.0.9

- v2.0.8の詳細コードレビューに基づく安定化・堅牢化更新。DSPの基本数式と既存Room／StadiumのIR生成は変更していない。
- 標準処理開始時に補正・リバーブ設定をスナップショット化し、処理途中のUI変更が実行中DSPへ混入しないようにした。
- 標準処理中は標準／バッチ双方の共有設定、入力ファイル、postfix、保存モード等をロックし、同時バッチ開始を防止。
- 最後に適用した処理設定を`state.appliedSettings`として保持。未適用変更がある間は単曲ダウンロードを無効化し、設定値リストは適用済み設定と保留中設定を分離して記録。
- 設定値リストのピーク／正規化見込みを最終リバーブ後音源基準へ修正し、リバーブ全設定、処理モード、プリセットID、出力設定を完全出力。
- バッチ開始時に入力ファイル一覧と保存先を固定。処理中のファイル再選択による対象・進捗・CSVの不整合を防止。
- バッチ中止時に未処理の残り全ファイルをSKIPとして一覧とCSVへ記録。
- 自動トリムの出力尺をフルテール保持時の長さ以下へ制限。
- 補正OFF時に旧基準音源を破棄し、差分波形へ古い状態が残らないよう修正。
- ユーザープリセットJSONを型・有限値・範囲・許可値で正規化し、未知プロファイルはRoom、未知テール方式は原音尺維持へフォールバック。
- localStorage保存失敗を黙殺せず、セッション内反映と永続保存失敗を明示。
- WAVパーサーへRIFF／チャンク境界、fmt、data、blockAlign、対応形式等の検証を追加。dataチャンクがfmtより先にある有効WAVにも対応。
- WAVエンコード前の音声構造・RIFF上限検証を追加。
- CSVセル先頭の`=`, `+`, `-`, `@`による表計算ソフトの式解釈を防止。
- 整数PCM出力時に範囲外サンプル数を検出し、単曲保存とバッチCSVへクリッピング警告を追加。
- A/B停止中の波形カーソル位置参照と、全体リセット時のAudio要素解放を修正。
- Room／StadiumのBlueprintおよびIRはv2.0.8と全サンプル一致。固定音源の最終出力差は最大`2.98e-8`。
- 6プロファイル、Wet 0%、mono／stereo、4処理モード、単曲／バッチ、テール、例外入力をブラウザ回帰試験。

## v2.0.8

- 詳細リバーブ設定に空間プロファイル選択を追加。
  - Room
  - Studio
  - Plate
  - Hall
  - Stadium
  - Church
- Studio、Plate、Hall、Church用の独自人工IR生成を追加。
- Room／Stadiumはv2.0.7の既存生成コードをそのまま呼び出し、既存音を維持。
- プロファイル変更時はリバーブプリセットを`カスタム（未保存）`へ変更し、自動再処理は行わない仕様とした。
- 現在のプロファイル説明を選択欄直下へ表示。日本語／英語切替に対応。
- 空間プロファイルをリバーブユーザープリセットへ保存・読込可能にした。欠落値・未知値はRoomへフォールバック。
- バッチ設定要約とCSVレポートへ`roomProfile`を追加。
- 設定値リストへリバーブ設定全体とLow／Mid／High各帯域の全設定を追加。
- 既存システムプリセットの数値・プロファイル割当は変更なし。既存の「ホール」プリセットもRoomのまま維持。

## v2.0.7

- バッチ処理タブへ移動した際、標準フローのA/Bプレビューが再生中なら自動停止するよう変更。
- 詳細設定・ログタブへの移動ではプレビュー再生を継続。

## v2.0.6

- 補正／リバーブのチェックを肯定形へ統一。
  - `補正を有効にする`
  - `リバーブを有効にする`
- 初期状態と全体リセット後は両方ON。
- `補正・リバーブを適用`のボタン名を固定。
- 補正OFF時は`基準カーブに戻す`を`入力音源に戻す`へ切替。
- 補正OFF・リバーブOFF時に入力スルー＋必要な正規化だけを行えるよう変更。
- 補正OFFで読み込んだ後、補正ONへ切り替えた場合に基準カーブを自動生成。
- 補正／リバーブのプリセットUIを、選択＋追加、保存＋読み込み、説明の構成へ整理。

## v2.0.5

- 補正をバイパスし、リバーブだけを適用できる処理モードを追加。
- 標準フローとバッチ処理で補正有効状態を同期。
- 補正OFF時は基準カーブ解析・生成を省略。
- バッチCSVへ処理モードを記録。
- 詳細リバーブ設定のLow / Mid / Highを縦一列へ変更。

## v2.0.4

- 基準カーブ状態へのリバーブ適用を廃止。
  - 音源読み込み時は基準カーブ適用音源まで生成。
  - リバーブは`補正・リバーブを適用`時だけ実行。
- 検証過程で追加したバッチ補正状態、保存後WAV再検証、フレーム照合等のデバッグ処理を除去。
- バッチ処理を通常の整形補正→リバーブ→正規化フローへ整理。
- 詳細設定に`MPC-BE互換性ガード（16-bit PCM）`を追加。初期値OFF。
- 16-bit PCMデータ内の偶発的なDTS同期ワードを検出し、該当時のみ1 LSB調整可能にした。

## v2.0.3

- バッチ処理の補正状態を明示する検証機能を追加。
- 基準カーブ／整形補正の出力長差を調査するため、フレーム数等をCSVへ追加。
- 後にv2.0.4で検証用機能を整理・撤去。

## v2.0.2

- バッチ直接保存経路の診断用検証を追加。
- 保存後WAVのRIFF / WAVE / fmt / dataサイズ照合を追加。
- 後に原因が保存処理ではないと判明し、v2.0.4で撤去。

## v2.0.1

- 標準フローとバッチの補正状態差を調査するための同期処理を追加。
- 後にMPC-BEの再生問題とは無関係と判明。

## v2.0.0

- メジャーバージョン更新。
- マルチバンド・マスタリング・リバーブをMEL本体へ統合。
- 処理順をレベリング→ポスト飛び出し圧縮→リバーブ→最終測定→正規化へ設定。
- Dry信号は未分割のまま維持し、Wet送りのみLow / Mid / Highへ分割。
- 共通の人工空間構造を使い、帯域別Wet、プリディレイ、ディケイ、ダンピング、ステレオ幅を実装。
- リバーブシステムプリセットを追加。
  - 標準
  - 微細な一体感
  - ボーカルの空気感
  - 広がり
  - ライブハウス
  - スタジアム
  - ホール
- 補正プリセットとリバーブプリセットを分離。
- 複数ユーザープリセットの追加・削除・JSON保存・統合読み込みを実装。
- 全体Wet倍率を標準フローへ追加。
- 連動A/Bプレビューへ更新。
- レベリング後ピーク／LUFSと最終処理後ピーク／LUFSを分離表示。
- リバーブテールによる出力時間延長と追加秒数を表示。
- 単曲処理とバッチ処理の両方へリバーブを統合。
- バッチCSVへリバーブ設定、入出力時間、テール、リバーブ後ピーク／LUFSを追加。
- バージョン体系を2.0.0系へ移行。

## v1.2.0

- File System Access APIを使ったバッチフォルダ直接保存。
- 複数ファイル逐次処理。
- バッチCSVレポート。
- ラウドネス正規化とIntegrated LUFS参考値。
- 単曲出力形式とバッチ出力形式を分離。
- 日本語／英語UI。
- 補正ユーザープリセットJSON。

## v1.0系

- 短期絶対ピーク包絡から基準カーブを生成する方式へ移行。
- トゲ除去、メディアン処理、平滑化、スルーレート制限を実装。
- ポスト飛び出し圧縮を実装。
- 基準カーブ状態と整形補正状態のプレビュー・保存を実装。
- 波形本体canvasとカーソルcanvasを分離し、再生性能を改善。
